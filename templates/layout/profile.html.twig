<!-- User Dropdown Menu -->
<li class="nav-item dropdown user-menu">
    <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown">
        <img src="{% if app.user.profile.image %} {{ asset('uploads/images/' ~ app.user.profile.image) }} {% else %} {{ asset('adminltev3/dist/img/avatar.png') }} {% endif %}" class="user-image img-circle elevation-2" alt="User Image">
        <span class="d-none d-md-inline">{% if app.user.profile.firstname %} {{ app.user.profile.firstname }} {{ app.user.profile.lastname }} {% else %} {{ app.user.email }} {% endif %}</span>
    </a>
    <ul class="dropdown-menu dropdown-menu-lg dropdown-menu-right">
        <!-- User image -->
        <li class="user-header bg-primary">
            <img src="{% if app.user.profile.image %} {{ asset('uploads/images/' ~ app.user.profile.image) }} {% else %} {{ asset('adminltev3/dist/img/avatar.png') }} {% endif %}" class="img-circle elevation-2" alt="User Image">

            <p>
                {% if app.user.profile.firstname %} {{ app.user.profile.firstname }} {{ app.user.profile.lastname }} {% else %} {{ app.user.email }} {% endif %} - Web Developer
                <small>Member since Nov. 2012</small>
            </p>
        </li>
        <!-- Menu Body -->
        <li class="user-body">
            <div class="row">
                <div class="col-4 text-center">
                    <a href="#">Followers</a>
                </div>
                <div class="col-4 text-center">
                    <a href="#">Sales</a>
                </div>
                <div class="col-4 text-center">
                    <a href="#">Friends</a>
                </div>
            </div>
            <!-- /.row -->
        </li>
        <!-- Menu Footer-->
        <li class="user-footer">
            <a href="{{ path('profile_index') }}" class="btn btn-default btn-flat">Profile</a>
            <a href="{{ path('app_logout') }}" class="btn btn-default btn-flat float-right">Sign out</a>
        </li>
    </ul>
</li>
